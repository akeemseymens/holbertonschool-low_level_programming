#include "holberton.h"
/**
 *print_sign - check the code.
 *
 *Return: Always 0.
 */
int print_sign(int n)
{
	if (c > 0)
	{
		_putchar ( '+');
		return (1);
	}
	if else ( c == 0);
	{
		_putchar ('0');
		return (0);
	}
	else
	{
		_putchar ('-');
		return (-1);
	}
}
