/**
 * main - Entry point
 * Description -print alphabet and go past e and q.
 * Return: Always 0 (Success)
 */
#include <stdio.h>

int main(void)
{
	int n;
	char c;

	for (n = 0 ; n < 10 ; n++)
	{
		putchar(i + '0');
	}
	for (c = 'a';c < 'g'; c++)
	{
		putchar(c);
	}

	putchar('\n');

	return (0);
}
