/**
 * main - Entry point
 * Description -print alphabet and go past e and q.
 * Return: Always 0 (Success)
 */
#include <stdio.h>

int main(void)
{
	int n;

	for (n = 0 ; n <= 9 ; n++)
	{
		printf("%i", n);
	}

	putchar('\n');

	return (0);
}
